<%= form_for(number) do |f| %>
  <% if number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number.errors.count, "error") %> prohibited this number from being saved:</h2>

      <ul>
      <% number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hours %>
    <%= f.number_field :hours %>
  </div>

    <%= f.hidden_field :participation_key %>
    <%= f.hidden_field :poll_id %>

  <div class="actions">
    <%= f.submit t('.submit')%>
  </div>
<% end %>
